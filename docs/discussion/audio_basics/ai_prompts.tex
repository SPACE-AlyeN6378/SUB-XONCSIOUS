\promptitem{prompt:example_dsp_plot}{%
    ``Generate two plots, a smooth continous plot and a discrete-time stem plot 
    of a sine wave, describing how the wave is quantized.''
}

\promptitem{prompt:example_float_values}{%
    ``Can you retrieve the list of floating point values from this plot?''
}

\promptitem{prompt:latency_factors}{
    ``Summarize what could be the causes which lead to latency? 
    (\textit{Link to \cite{burg2014latency} prompted})

I mean there's buffer size, ADC and DAC. But are there anything else?''
}

\promptitem{prompt:execution_time}{
    ``You previously mentioned that the CPU usage is determined by 
    Callback time divided by buffer time (i.e. the deadline). Is that 
    some sort of computational time requirement?''
}

\promptitem{prompt:stddev}{
    ``What formula should I use to calculate standard deviation? I'm 
    expecting the mean of squares and the square of means.''
}

\promptitem{prompt:chronovssample}{
    ``Why do we use samples for real-time scheduling instead of std::chrono?''
}