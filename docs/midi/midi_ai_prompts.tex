\promptitem{prompt:mixing}{%    https://chatgpt.com/share/6a5d74f1-86c0-83e8-a982-863e91c9b322
    ``I read the textbook on "Designing Audio Effect Plugins in C++" and found out that one thread handles the audio
    and the other for GUI independently. How must the final audio be mixed? Should I use `for' loop to go through 
    each track and sum up each sample, or just allocate each track per thread and compute the sum in parallel?''
}
